package sqlGenerator;

public class Parser {
/*
 * For each year, for each game, add board with move plus the 2 rotations and the flip, then do move.
 * 
 * To store, board, whose turn, move (player who took it), opponent, year, winner, score
 */
}
